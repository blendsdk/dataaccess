import { DataFactory } from "@blendsdk/datafactory";
import { <%=interfaceName%> } from "../DbTypes";

/**
 * Generated base class for <%=tableName%> factory
 *
 * @export
 * @abstract
 * @class <%=className%>
 * @extends {Factory}
 */
export abstract class <%=className%> extends DataFactory {
	/**
	 * Select all record of the <%=tableName%> table.
	 *
	 * @returns {Promise<<%=interfaceName%>>}
	 */
	public getAll(): Promise<<%=interfaceName%>[]> {
		return this.executeQuery("SELECT * FROM <%=tableName%>");
	}
<% methods.forEach(method => {%>
<%-method -%>
<%});%>
}
